<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.soft.study.dao.QuizDao">


    <select id="list" resultType="kr.soft.study.dao.QuizDao">
        SELECT * FROM OXQuizzes
    </select>

    <select id="findById" parameterType="int" resultMap="quizResultMap">
        SELECT * FROM OXQuizzes WHERE id = #{id}
    </select>

    <insert id="insertQuiz" parameterType="Quiz">
        INSERT INTO OXQuizzes (category, question, answer, points, explanation)
        VALUES (#{category}, #{question}, #{answer}, #{points}, #{explanation})
    </insert>

    <update id="updateQuiz" parameterType="Quiz">
        UPDATE OXQuizzes SET
        category = #{category},
        question = #{question},
        answer = #{answer},
        points = #{points},
        explanation = #{explanation}
        WHERE id = #{id}
    </update>

    <delete id="deleteQuiz" parameterType="int">
        DELETE FROM OXQuizzes WHERE id = #{id}
    </delete>

</mapper>
